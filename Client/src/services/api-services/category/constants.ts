export const Category_Create = "category";
export const Category_GetAll = "category";
export const Category_GetById = (category_id: number) =>
  `category/${category_id}`;
export const Category_Update = (category_id: number) =>
  `category/${category_id}`;
export const Category_Delete = (category_id: number) =>
  `category/${category_id}`;
